---
name: Renovate

on:
  workflow_dispatch:
  schedule:
    - cron: "0 */6 * * *"

permissions:
  contents: read

jobs:
  renovate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.2

      - name: Renovate
        uses: renovatebot/github-action@v46.1.14
        with:
          token: ${{ secrets.RENOVATE_TOKEN }}
        env:
          RENOVATE_ALLOWED_COMMANDS: '["^bash \\.renovate/scripts/update-alpine-apk-sha\\.sh$"]'
          RENOVATE_REPOSITORIES: ${{ github.repository }}
