---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: ${APP}-cnpg
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: ${APP}-cnpg
  podMetricsEndpoints:
    - port: metrics
