iptables -I OUTPUT 1 -d ${VPN_DNS_SERVICE_IP:=172.30.0.10} -p udp --dport 53 -j $(if [ "${VPN_CLUSTER_DNS_ENABLED:=false}" = "true" ]; then printf ACCEPT; else printf DROP; fi)
iptables -I OUTPUT 2 -d ${VPN_DNS_SERVICE_IP:=172.30.0.10} -p tcp --dport 53 -j $(if [ "${VPN_CLUSTER_DNS_ENABLED:=false}" = "true" ]; then printf ACCEPT; else printf DROP; fi)
iptables -I OUTPUT 3 -d ${VPN_DNS_SERVICE_IP:=172.30.0.10} -j DROP
