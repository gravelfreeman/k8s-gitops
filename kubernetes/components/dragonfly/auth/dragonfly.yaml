---
apiVersion: dragonflydb.io/v1alpha1
kind: Dragonfly
metadata:
  name: ${APP}-dragonfly
spec:
  authentication:
    passwordFromSecret:
      name: ${DRAGONFLY_AUTH_SECRET_NAME:=${APP}-app-secret}
      key: ${DRAGONFLY_AUTH_SECRET_KEY:=DRAGONFLY_KEY}
