---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: ${APP}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: ${APP}-agent-reader
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    resourceNames: ["${APP}-agent"]
    verbs: ["get"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: ${APP}-agent-reader
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: ${APP}-agent-reader
subjects:
  - kind: ServiceAccount
    name: gluetun-router
    namespace: gluetun
