---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &app homepage
spec:
  commonMetadata:
    labels:
      app.kubernetes.io/name: *app
  components:
    - ../../../../components/common
    - ../../../../components/envoy-oidc
    - ../../../../components/volsync
  dependsOn:
    - name: envoy-gateway-config
    - name: external-secrets-store
    - name: rook-ceph-cluster
    - name: volsync
  interval: 1h
  path: kubernetes/apps/services/homepage/app
  postBuild:
    substitute:
      APP: *app
      ENVOY_OIDC_SUBDOMAIN: root
      VOLSYNC_CACHE_CAPACITY: 128Mi
      VOLSYNC_CAPACITY: 256Mi
      VOLSYNC_CLAIM: images
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  targetNamespace: *app
  timeout: 5m
  wait: true
