apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helmrelease.yaml
configMapGenerator:
  - name: openchore-nginx
    files:
      - default.conf=./resources/default.conf
generatorOptions:
  disableNameSuffixHash: true
