listen irc+insecure://0.0.0.0:6697
# v0.11.0+: replace the localhost listener with this pod-wide listener.
# listen http+insecure+prometheus://0.0.0.0:8080
listen http+prometheus://localhost:8080
hostname irc.${DOMAIN_0}
title "Freeman IRC"
db postgres "host=soju-cnpg-rw.soju.svc.cluster.local dbname=soju sslmode=require"
message-store db
