---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: home-assistant
spec:
  interval: 30m
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system
  values:
    controllers:
      home-assistant:
        pod:
          annotations:
            k8s.v1.cni.cncf.io/networks: '[{"name":"iot","namespace":"multus","ips":["10.0.30.30/24"],"mac":"02:0a:00:1e:00:7b"}]'
        containers:
          app:
            image:
              repository: ghcr.io/home-operations/home-assistant
              tag: 2026.9.1@sha256:1bcf770d15bd91a55491d04f12494febb35da68b025e1f6612a167a2e190a8c7
            args: [--log-file, /tmp/home-assistant.log]
            probes:
              liveness:
                enabled: true
              readiness:
                enabled: true
              startup:
                enabled: true
                spec:
                  periodSeconds: 10
                  failureThreshold: 30
            resources:
              requests:
                cpu: '1'
                memory: 4Gi
              limits:
                memory: 8Gi
          git-sync:
            image:
              repository: registry.k8s.io/git-sync/git-sync
              tag: v4.7.1@sha256:c5e1976da735bf0b53979f079a07475d77174c51863cd084e928ff6074480717
            args:
              - --repo=https://github.com/gravelfreeman/ha-gitops
              - --ref=main
              - --root=/config/.git-sync
              - --sparse-checkout-file=/etc/git-sync/sparse-checkout
              - --exechook-command=/etc/git-sync/exechook.sh
              - --verbose=6
            resources:
              requests:
                cpu: 10m
                memory: 64Mi
              limits:
                memory: 128Mi
        defaultContainerOptions:
          securityContext:
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop: [ALL]
    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsGroup: 568
        runAsUser: 568
        fsGroup: 568
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile:
          type: RuntimeDefault
    persistence:
      config:
        existingClaim: home-assistant-data
      secrets:
        type: secret
        name: home-assistant-app-secret
        advancedMounts:
          home-assistant:
            app:
              - path: /config/secrets.yaml
                subPath: secrets.yaml
      workdir:
        type: emptyDir
        advancedMounts:
          home-assistant:
            app:
              - path: /config/.venv
      git-sync-config:
        type: configMap
        name: git-sync-config
        defaultMode: 0555
        advancedMounts:
          home-assistant:
            git-sync:
              - path: /etc/git-sync
      git-sync-secret:
        type: secret
        name: git-sync-secret
        advancedMounts:
          home-assistant:
            git-sync:
              - path: /run/secrets/git-sync-secret
    route:
      app:
        hostnames: ['home.${DOMAIN_0}']
        parentRefs:
          - name: envoy-internal
            namespace: envoy-gateway
            sectionName: https
    service:
      app:
        ports:
          http:
            port: 8123
