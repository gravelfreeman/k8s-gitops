---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: deleterr
spec:
  interval: 30m
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system
  values:
    controllers:
      deleterr:
        type: cronjob
        cronjob:
          backoffLimit: 0
          schedule: "0 2 * * *"
        containers:
          app:
            image:
              repository: docker.io/rfsbraz/deleterr
              tag: 0.2.23@sha256:b6f4c0757aa49c32f396459b300d96566c6fec7b34495a0ef6a7441f02eb646f
            env:
              PLEXAPI_CONFIG_PATH: /config/plexapi.ini
            envFrom:
              - secretRef:
                  name: deleterr-app-secret
            resources:
              requests:
                cpu: 250m
                memory: 256Mi
              limits:
                memory: 1Gi
            securityContext:
              readOnlyRootFilesystem: true
              allowPrivilegeEscalation: false
              capabilities:
                drop: ["ALL"]
        pod:
          securityContext:
            runAsNonRoot: true
            runAsUser: 568
            runAsGroup: 568
            fsGroup: 568
            fsGroupChangePolicy: OnRootMismatch
            seccompProfile:
              type: RuntimeDefault
    persistence:
      workdir:
        type: emptyDir
        globalMounts:
          - path: /config
          - path: /config/logs
      settings:
        type: configMap
        name: settings
        globalMounts:
          - path: /config/settings.yaml
            subPath: settings.yaml
