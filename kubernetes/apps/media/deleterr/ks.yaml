---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &app deleterr
spec:
  commonMetadata:
    labels:
      app.kubernetes.io/name: *app
  components:
    - ../../../../components/common/app
  dependsOn:
    - name: clusterplex
    - name: external-secrets-store
    - name: radarr
    - name: seerr
    - name: sonarr
  interval: 1h
  path: kubernetes/apps/media/deleterr/app
  postBuild:
    substitute:
      APP: *app
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  targetNamespace: *app
  timeout: 5m
  wait: true
